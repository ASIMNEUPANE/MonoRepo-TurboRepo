
import styles from "./page.module.css";
import { Button } from "@repo/ui/button";

function Gradient(): JSX.Element {
  return (
    <div>
      <Button appName="web app" children={undefined}/>
      </div>
  );
}

export default Gradient